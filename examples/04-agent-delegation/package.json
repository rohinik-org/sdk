{
  "name": "04-agent-delegation",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Observe agent delegation events in a multi-agent execution.",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@rohinik-org/client": "^1.0.0",
    "@rohinik-org/testing": "^0.16.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
