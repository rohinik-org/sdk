/**
 * Capability context visible to a capability implementation.
 *
 * Re-exported from @rohinik-org/capability-manifest (driver-level ExecutionContext).
 * No runtime-internal objects here — no registries, no kernel, no service locators.
 */
export type { ExecutionContext as CapabilityContext } from '@rohinik-org/capability-manifest'
