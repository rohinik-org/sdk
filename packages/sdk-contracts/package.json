{
  "name": "@rohinik-org/sdk-contracts",
  "version": "0.15.0",
  "private": true,
  "type": "module",
  "description": "Rohinik SDK — authoring contracts and matcher infrastructure for adapter and asset authors",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "*",
    "typescript": "*",
    "vitest": "*"
  }
}
